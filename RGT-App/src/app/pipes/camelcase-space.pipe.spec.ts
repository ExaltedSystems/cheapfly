import { CamelcaseSpacePipe } from './camelcase-space.pipe';

describe('CamelcaseSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CamelcaseSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
